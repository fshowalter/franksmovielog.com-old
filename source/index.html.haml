
%main
  %ul#list.timeline
    - posts.each do |post|
      %li.event{ 'data-date': post.date.iso8601 }
        %h3.events-heading
          %a{ href: post.link }
            = post.title
          %p.event-slug
            #{post.date.strftime("%B #{post.date.day.ordinalize}")}<span class="js-hide"> #{post.date.year}</span> on #{post.location}
